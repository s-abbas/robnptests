#' @param scale a character string specifying the scale estimator used for standardization
#'              of the test statistic, must be one of \code{"S1"} and \code{"S2"}.
#'              Default is \code{"S1"}. Ignored if \code{method = "asymptotic"};
#'              see details for definition of the scale estimators.
