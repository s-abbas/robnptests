#' @param seed Optional, numerical value used as a seed for wobbling of the
#'             samples. The argument is ignored if \code{wobble = FALSE}. If no
#'             value is given, a random number is used. The seed  will be
#'             returned in a warning message.
