#' @param na.rm a logical value indicating whether NA values in \code{x} and
#'              \code{y} should be stripped before the computation proceeds. The
#'              default is \code{na.rm = FALSE}.
